# Required only when Hub upload is enabled (`--no-push-to-hub` is not set).
HF_TOKEN=your_huggingface_token_here

# Optional for Weights & Biases logging.
# You can set either WANDB_API_KEY or WANDB_TOKEN.
WANDB_API_KEY=your_wandb_api_key_here
WANDB_TOKEN=
